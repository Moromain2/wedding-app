<h1>Hebergements</h1>

<% if @hebergements.any? %>
  <ul>
  <% @hebergements.each do |hebergement| %>
    <li><%= link_to hebergement.title, hebergement_path(hebergement) %></li>
  <% end %>
  </ul>
<% else %>
  <h4>Pas encore de hebergements pour le moment, ça arrive vite 😉</h4>
<% end %>
