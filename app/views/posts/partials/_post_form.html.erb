<%= form_for @post do |f| %>
  <div class="form-wrapper">
    <%= f.label :title, 'Titre *' %><br>
    <%= f.text_field :title, :required => true, placeholder: "Requis" %><br>
    <br>

    <%= f.label :body, 'Contenu *' %><br>
    <%= f.text_area :body, :required => true, placeholder: "Requis"%><br>
    <br>

    <% if current_page?(action: 'new') %>
      <%= f.submit 'Ajouter' %>
    <% end %>
    <% if current_page?(action: 'edit') %>
      <%= f.submit 'Mettre à Jour' %>
      <%= link_to 'Effacer', @post, method: :delete, data: {confirm: 'Veux tu vraiment effacer ce post ?'} %>
    <% end %>
  </div>
<% end %>
