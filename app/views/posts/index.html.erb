<div class="page-wrapper">
  
  <h1>Tous les posts</h1>

  <% if @posts.any? %>
    <ul>
    <% @posts.each do |post| %>
      <li><%= link_to post.title, post_path(post) %></li>
    <% end %>
    </ul>
  <% else %>
    <h4>Pas encore de posts pour le moment, ça arrive vite 😉</h4>
  <% end %>
  
</div>
